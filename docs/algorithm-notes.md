# Algorithm Notes

- 線形探索は先頭から順に比較する。配列が未整列でも扱いやすい。
- 二分探索は整列済み配列を前提に探索範囲を半分ずつ狭める。
- 選択ソートは最小値を見つけて前へ送る考え方の導入に向く。
- クイックソートは高速だが説明コストが高いため、この MVP ではノート中心に扱う。
- 状態可視化がなくても trace 出力で比較回数や更新順を確認できる設計にする。
